pipeline {
  agent any
  triggers{
    githubPush()
  }
  stages {
    stage('Docker Build'){
      steps {
        bat 'docker build -t onlinestore .'
      }
    }
    stage('Docker Run'){
      steps{
        bat 'docker run -d -p 3001:3001 onlinestore'
      }
    }
    stage('Final'){
      steps{
        bat 'docker ps'
      }
    }
    stage('Deploy') {
            steps {
                echo 'Deploying application...'
            }
        }
    }
}